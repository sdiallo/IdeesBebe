.reply
  %h3 Commentaires

  = simple_form_for @comment, url: product_comments_path(@product.slug) do |f|
    = f.input :content, label: I18n.t('common.comment')
    .actions
      = f.submit I18n.t('common.save')
